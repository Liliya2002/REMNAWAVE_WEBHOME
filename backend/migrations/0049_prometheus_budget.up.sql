-- PROMETHEUS: потолок расхода на один разбор, задаваемый владельцем.
--
-- Потолок был зашит в коде и молча не работал: он считался по usage из ответа
-- провайдера, а шлюз его не возвращает — счётчик стоял на нуле, и разбор шёл до
-- упора в число шагов. Один такой обошёлся в 600 тысяч токенов и закончился
-- словами «исчерпан лимит шагов» вместо ответа.
--
-- Теперь предел в настройках: это деньги владельца, и порог ему виднее.
ALTER TABLE prometheus_settings
  ADD COLUMN IF NOT EXISTS token_budget INTEGER NOT NULL DEFAULT 150000;

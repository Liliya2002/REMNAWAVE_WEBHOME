-- v0.1.9: опциональный приватный SSH-ключ.
-- Без приватника наша система не может SSH-ить в созданные VM. Если юзер хочет
-- управлять VM через /admin/vps — он добавляет приватник один раз, мы его
-- шифруем и используем (не показываем больше никогда).

ALTER TABLE yc_ssh_keys
  ADD COLUMN IF NOT EXISTS private_key TEXT;
